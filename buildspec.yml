version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20
      nodejs: 18
      
  build:
    commands:
      - echo "Setting up GitHub Actions self-hosted runner"
      - echo "The actual build will be handled by GitHub Actions"

artifacts:
  files:
    - '**/*'